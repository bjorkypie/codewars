/*

Convert Boolean Values to Strings 'Yes' or 'No' - 8kyu

Descripton:

Complete the method that takes a boolean value and return a 'Yes' string for true, or  'No' string for false.

*/

function boolToWord( bool ){
  //conditional
  return bool === true ? 'Yes' : 'No';
}
